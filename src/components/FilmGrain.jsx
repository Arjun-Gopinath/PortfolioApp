// Page-wide film grain — the cinematic counterpart to the hero pitch-lines.
// Purely decorative; hidden from assistive tech and disabled under reduced
// motion via the .film-grain CSS rules.
const FilmGrain = () => <div className="film-grain" aria-hidden="true" />;

export default FilmGrain;
